# Phase 1.8: 移动端功能增强 - 总览

## 1. 功能概述与业务场景

### 1.1 功能概述

本模块在现有移动端功能基础上，增强离线工作能力和移动审批能力，支持在无网络环境下进行资产盘点、数据采集，并在网络恢复后自动同步数据。同时完善移动端审批流程，支持流程图可视化、批量审批、代理审批等功能。

核心功能包括：

1. **设备管理**：支持多设备绑定、设备解绑、设备安全设置（生物识别、离线权限）
2. **离线同步**：基于版本向量的数据同步机制，支持离线操作和自动冲突解决
3. **移动审批**：优化的移动端审批流程，支持批量审批、审批代理、审批委托
4. **数据压缩**：减少流量消耗，提升移动端性能
5. **安全增强**：设备绑定、生物识别、操作日志记录

### 1.2 业务场景

| 场景 | 用户角色 | 业务流程 | 预期效果 |
|------|---------|---------|---------|
| **移动盘点** | 盘点员 | 扫描二维码→离线记录→批量上传→自动同步 | 无网络环境下可正常盘点，提高盘点效率 |
| **移动审批** | 审批人 | 接收通知→查看详情→快速审批→转办/驳回 | 随时随地处理审批，缩短审批周期 |
| **设备管理** | 系统管理员 | 设备注册→设置权限→监控状态→解绑设备 | 确保设备安全，防止未授权访问 |
| **离线操作** | 外勤人员 | 下载基础数据→离线操作→网络恢复→自动同步 | 适应各种网络环境，保证业务连续性 |
| **数据冲突** | 多用户 | 同时编辑→版本检测→智能合并→手动解决 | 保证数据一致性，避免数据丢失 |

## 2. 用户角色与权限

| 角色 | 权限说明 | 主要操作 |
|------|---------|---------|
| **普通用户** | 绑定个人设备、使用移动端功能、离线操作 | 设备注册、离线扫描、移动审批、查看我的数据 |
| **盘点员** | 移动盘点、离线盘点、批量上传 | 盘点任务、扫描资产、上传盘点数据 |
| **审批人** | 移动审批、批量审批、设置代理 | 待办审批、批量审批、设置代理、查看审批历史 |
| **系统管理员** | 设备管理、查看安全日志、配置同步策略 | 设备绑定/解绑、查看日志、配置离线策略 |
| **财务人员** | 移动端查看资产统计、审批预算 | 查看报表、移动审批、预算管理 |

## 3. 公共模型引用声明

本模块严格遵循GZEAMS公共基类架构规范，所有后端组件均继承相应的公共基类：

| 组件类型 | 基类 | 引用路径 | 自动获得功能 |
|---------|------|---------|-------------|
| Model | BaseModel | apps.common.models.BaseModel | 组织隔离（org字段）、软删除（is_deleted+deleted_at）、审计字段（created_at+updated_at+created_by）、动态扩展（custom_fields JSONField） |
| Serializer | BaseModelSerializer | apps.common.serializers.base.BaseModelSerializer | 自动序列化公共字段、custom_fields处理 |
| Serializer (带审计) | BaseModelWithAuditSerializer | apps.common.serializers.base.BaseModelWithAuditSerializer | 包含updated_by和deleted_by的完整审计信息 |
| ViewSet | BaseModelViewSetWithBatch | apps.common.viewsets.base.BaseModelViewSetWithBatch | 组织过滤、软删除过滤、自动设置审计字段、批量操作、已删除记录查询 |
| Filter | BaseModelFilter | apps.common.filters.base.BaseModelFilter | 公共字段过滤、时间范围查询 |
| Service | BaseCRUDService | apps.common.services.base_crud.BaseCRUDService | 统一CRUD方法、自动组织隔离、软删除处理、批量操作支持 |

**核心模型继承关系**：

```python
# 所有移动端模型继承BaseModel
from apps.common.models import BaseModel

class MobileDevice(BaseModel):
    """移动设备管理 - 自动获得组织隔离、软删除、审计、自定义字段"""
    user = models.ForeignKey(settings.AUTH_USER_MODEL, on_delete=models.CASCADE)
    device_id = models.CharField(max_length=200, unique=True)
    # ...

class SyncQueue(BaseModel):
    """同步队列 - 自动获得组织隔离、软删除、审计、自定义字段"""
    device = models.ForeignKey(MobileDevice, on_delete=models.CASCADE)
    table_name = models.CharField(max_length=100)
    # ...

class SyncConflict(BaseModel):
    """同步冲突 - 自动获得组织隔离、软删除、审计、自定义字段"""
    device = models.ForeignKey(MobileDevice, on_delete=models.CASCADE)
    table_name = models.CharField(max_length=100)
    # ...

class ApprovalDelegate(BaseModel):
    """审批代理 - 自动获得组织隔离、软删除、审计、自定义字段"""
    delegator = models.ForeignKey(settings.AUTH_USER_MODEL, on_delete=models.CASCADE)
    delegate = models.ForeignKey(settings.AUTH_USER_MODEL, on_delete=models.CASCADE)
    # ...
```

## 4. 数据模型设计

详见 [backend.md](./backend.md) - 包含完整的数据模型定义、序列化器、ViewSet、Service层设计

## 5. API接口设计

详见 [api.md](./api.md) - 包含完整的REST API接口定义、请求响应格式、错误码定义

## 6. 前端组件设计

详见 [frontend.md](./frontend.md) - 包含Vue 3组件实现、移动端适配、IndexedDB集成

## 7. 测试用例

详见 [test.md](./test.md) - 包含单元测试、集成测试、离线同步测试

---

## 8. 业务背景

### 1.1 当前痛点

| 痛点 | 说明 | 影响 |
|------|------|------|
| **依赖网络** | 无网络环境无法进行盘点操作 | 仓库、地下室等场景受限 |
| **数据易丢失** | 操作中断后数据未保存 | 需要重新操作 |
| **审批体验差** | 移动端审批功能简单 | 影响审批效率 |
| **无离线缓存** | 基础数据无法离线查看 | 每次都需要联网 |
| **同步冲突** | 多设备操作冲突处理不当 | 数据不一致 |

### 1.2 业务目标

- **离线盘点**: 支持完全离线的资产盘点操作
- **智能同步**: 网络恢复后自动同步，智能处理冲突
- **移动审批**: 完善的移动端审批体验
- **数据缓存**: 基础数据本地缓存，减少网络依赖
- **安全可靠**: 数据加密存储，防止泄露

---

## 2. 功能架构

```
┌─────────────────────────────────────────────────────────────────────────┐
│                         移动端功能增强                                   │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │                    离线工作模块 (Offline)                        │   │
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐             │   │
│  │  │  本地数据库  │  │  离线队列    │  │  冲突解决    │             │   │
│  │  │  IndexedDB  │  │  Queue      │  │  Conflict   │             │   │
│  │  └─────────────┘  └─────────────┘  └─────────────┘             │   │
│  └─────────────────────────────────────────────────────────────────┘   │
│                                                                         │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │                    数据同步模块 (Sync)                           │   │
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐             │   │
│  │  │  增量同步    │  │  冲突检测    │  │  断点续传    │             │   │
│  │  │  DeltaSync  │  │  Detect     │  │  Resume     │             │   │
│  │  └─────────────┘  └─────────────┘  └─────────────┘             │   │
│  └─────────────────────────────────────────────────────────────────┘   │
│                                                                         │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │                    移动审批模块 (Approval)                       │   │
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐             │   │
│  │  │  流程可视化  │  │  批量审批    │  │  代理审批    │             │   │
│  │  │  VisualFlow │  │  Batch      │  │  Delegate   │             │   │
│  │  └─────────────┘  └─────────────┘  └─────────────┘             │   │
│  └─────────────────────────────────────────────────────────────────┘   │
│                                                                         │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │                    安全模块 (Security)                           │   │
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐             │   │
│  │  │  数据加密    │  │  设备绑定    │  │  操作审计    │             │   │
│  │  │  Encryption │  │  BindDevice │  │  Audit      │             │   │
│  │  └─────────────┘  └─────────────┘  └─────────────┘             │   │
│  └─────────────────────────────────────────────────────────────────┘   │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 3. 离线工作模块

### 3.1 本地数据存储

| 功能 | 说明 |
|------|------|
| 本地数据库 | 使用IndexedDB存储基础数据和业务数据 |
| 数据结构 | 与后端保持一致的表结构 |
| 数据版本 | 记录数据版本号用于增量同步 |
| 存储限制 | 单表数据不超过100MB，总存储不超过50MB |

### 3.2 离线操作支持

| 操作 | 离线支持 | 说明 |
|------|----------|------|
| 资产盘点 | 完全支持 | 扫码、拍照、备注 |
| 资产查询 | 部分支持 | 基于缓存数据 |
| 数据提交 | 队列缓存 | 网络恢复后自动提交 |
| 审批操作 | 完全支持 | 支持离线审批 |

### 3.3 冲突解决策略

| 冲突类型 | 解决策略 |
|----------|----------|
| 同一记录多次修改 | 服务端优先（后写覆盖） |
| 离线期间记录被删除 | 保留离线修改，创建新记录 |
| 多设备离线修改 | 按时间戳合并，标记冲突 |
| 审批冲突 | 按审批流程顺序处理 |

---

## 4. 数据同步模块

### 4.1 同步类型

| 类型 | 触发时机 | 同步内容 |
|------|----------|----------|
| 全量同步 | 首次登录/数据重置 | 所有基础数据 |
| 增量同步 | 定时/网络恢复 | 变更数据 |
| 主动同步 | 用户手动触发 | 指定数据 |
| 后台同步 | 应用空闲时 | 静默同步 |

### 4.2 同步策略

```
同步流程:
┌──────────┐    ┌──────────┐    ┌──────────┐    ┌──────────┐
│ 检测网络  │ -> │ 获取变更  │ -> │ 上传数据  │ -> │ 下载更新  │
│ Network   │    │ Changes  │    │ Upload   │    │ Download  │
└──────────┘    └──────────┘    └──────────┘    └──────────┘
                                                ↓
                                          ┌──────────┐
                                          │ 处理冲突  │
                                          │ Conflict │
                                          └──────────┘
                                                ↓
                                          ┌──────────┐
                                          │ 更新本地  │
                                          │ Update   │
                                          └──────────┘
```

### 4.3 冲突检测与处理

```python
# 冲突检测规则
CONFLICT_RULES = {
    'version_mismatch': '本地版本与服务端版本不一致',
    'duplicate_create': '离线创建的记录在服务端已存在',
    'delete_modified': '离线期间记录被删除但本地有修改',
    'concurrent_modify': '多设备同时修改同一记录'
}

# 冲突解决策略
RESOLUTION_STRATEGIES = {
    'server_wins': '服务端数据优先',
    'client_wins': '客户端数据优先',
    'merge': '智能合并',
    'manual': '需用户手动选择'
}
```

---

## 5. 移动审批模块

### 5.1 审批功能增强

| 功能 | 说明 |
|------|------|
| 流程图可视化 | 展示完整审批流程和当前节点 |
| 批量审批 | 一次性审批多个待办 |
| 代理审批 | 设置代理人代为审批 |
| 审批备注 | 支持语音转文字备注 |
| 审批历史 | 查看完整审批记录 |

### 5.2 审批操作

| 操作 | 离线支持 |
|------|----------|
| 同意 | 是 |
| 拒绝 | 是 |
| 转交 | 是 |
| 撤回 | 否（需在线） |
| 加签 | 是 |

### 5.3 代理审批

```python
# 代理审批配置
DELEGATE_CONFIG = {
    'delegate_type': {
        'temporary': '临时代理，指定时间段',
        'permanent': '永久代理，直到取消'
    },
    'delegate_scope': {
        'all': '全部审批',
        'specific': '指定流程',
        'category': '指定类别'
    }
}
```

---

## 6. 安全模块

### 6.1 数据加密

| 数据类型 | 加密方式 |
|----------|----------|
| 本地数据库 | AES-256加密 |
| 敏感字段 | RSA+AES混合加密 |
| 传输数据 | HTTPS |
| 缓存数据 | 不包含敏感信息 |

### 6.2 设备管理

| 功能 | 说明 |
|------|------|
| 设备绑定 | 限制登录设备数量 |
| 设备解绑 | 远程解绑丢失设备 |
| 单点登录 | 新设备登录自动踢出旧设备 |
| 生物识别 | 支持指纹/面容解锁 |

### 6.3 操作审计

| 记录内容 | 说明 |
|----------|------|
| 登录记录 | 时间、地点、设备 |
| 操作记录 | 增删改查操作 |
| 同步记录 | 同步时间、数据量 |
| 审批记录 | 审批操作、意见 |

---

## 7. 数据模型关系

```
MobileDevice (移动设备)
    ├── user → User
    ├── device_id (设备唯一标识)
    ├── device_info (JSON)
    ├── last_sync_at
    └── is_bound

OfflineData (离线数据)
    ├── user → User
    ├── device → MobileDevice
    ├── table_name (表名)
    ├── record_id (记录ID)
    ├── operation (create/update/delete)
    ├── data (JSON)
    ├── status (pending/synced/conflict)
    └── created_at

SyncConflict (同步冲突)
    ├── user → User
    ├── table_name
    ├── record_id
    ├── local_data (JSON)
    ├── server_data (JSON)
    ├── conflict_type
    ├── resolution
    └── resolved_at

SyncLog (同步日志)
    ├── user → User
    ├── device → MobileDevice
    ├── sync_type (full/incremental)
    ├── sync_direction (upload/download)
    ├── record_count
    ├── status
    ├── error_message
    └── synced_at

ApprovalDelegate (审批代理)
    ├── delegator → User (委托人)
    ├── delegate → User (代理人)
    ├── delegate_type
    ├── start_time
    ├── end_time
    ├── scope (JSON)
    └── is_active
```

---

## 8. 与其他模块的集成

| 集成点 | 关联模块 | 集成方式 |
|--------|---------|---------|
| 资产盘点 | inventory | 离线扫码、缓存资产数据 |
| 工作流审批 | workflows | 移动审批、流程可视化 |
| 用户认证 | accounts | 设备绑定、生物识别 |
| 消息通知 | notifications | 同步状态通知 |
| 资产管理 | assets | 离线查询资产信息 |

---

## 9. 子文档索引

| 文档 | 说明 |
|------|------|
| [backend.md](./backend.md) | 后端实现 - 模型、同步服务、API |
| [api.md](./api.md) | API接口定义 |
| [frontend.md](./frontend.md) | 前端实现 - 离线存储、同步逻辑 |
| [test.md](./test.md) | 测试计划 |

---

## 10. 技术选型

### 10.1 移动端技术

| 技术 | 用途 |
|------|------|
| Vue 3 | 前端框架 |
| Vant 4 | UI组件库 |
| Dexie.js | IndexedDB封装 |
| Comlink | Web Worker支持 |
| Workbox | PWA支持 |

### 10.2 后端技术

| 技术 | 用途 |
|------|------|
| Django REST Framework | API框架 |
| Django Simple History | 数据版本追踪 |
| Celery | 异步任务处理 |

---

## 11. 后续任务

1. 实现离线数据存储层
2. 实现数据同步服务
3. 实现冲突检测与解决
4. 实现移动审批增强
5. 实现设备管理
6. 实现数据加密
7. PWA支持
8. 移动端测试
